# OS / tooling
.DS_Store
.pytest_cache/

# Python bytecode
__pycache__/
*.py[cod]

# Project virtualenv (symlink)
/irm

# Local docs / notebooks / reports attic — not version-controlled
/files/

# Run artifacts (grid searches, single-model runs, checkpoints)
/results/

.gitignore
